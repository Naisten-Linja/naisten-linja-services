import React from 'react';
import { RouteComponentProps } from '@reach/router';

export const FrontPage: React.FunctionComponent<RouteComponentProps> = () => {
  return <h1>Naisten Linja services</h1>;
};
